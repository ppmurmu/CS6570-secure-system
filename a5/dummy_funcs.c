// Auto‑generated dummy functions
#include <stddef.h>
#include <stdint.h>

__attribute__((used)) static void key_8519(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_8519(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_5998(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_5998(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_3480(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_3480(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_6739(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_6739(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_9943(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_9943(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_6751(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_6751(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_3721(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_3721(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_4010(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_4010(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_7928(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_7928(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_8252(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_8252(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_8680(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_8680(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_1291(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_1291(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_2814(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_2814(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_6777(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_6777(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_9820(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_9820(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_5422(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_5422(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_9850(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_9850(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_4134(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_4134(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_7282(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_7282(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_5329(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_5329(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_6582(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_6582(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_7031(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_7031(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_1059(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_1059(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_6154(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_6154(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_6221(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_6221(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_8907(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_8907(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_1183(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_1183(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_5785(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_5785(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_8411(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_8411(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

__attribute__((used)) static void key_4536(
    const uint8_t *obf,
    uint8_t *out,
    size_t len,
    uint8_t mask)
{
    // dummy XOR loop
    for (size_t i = 0; i < len; i++) {
        out[i] = obf[i] ^ mask;
    }
}

__attribute__((used)) static void egg_4536(void)
{
    // dummy work to confuse decompiler
    volatile int x = 0;
    for (int i = 0; i < 64; i++) {
        x += (i * 7) ^ 0x5F;
    }
}

// Optional: a table of pointers to all dummy funcs
typedef void (*func_t)(void);
func_t dummy_table[] = {
    (func_t)key_8519,
    egg_8519,
    (func_t)key_5998,
    egg_5998,
    (func_t)key_3480,
    egg_3480,
    (func_t)key_6739,
    egg_6739,
    (func_t)key_9943,
    egg_9943,
    (func_t)key_6751,
    egg_6751,
    (func_t)key_3721,
    egg_3721,
    (func_t)key_4010,
    egg_4010,
    (func_t)key_7928,
    egg_7928,
    (func_t)key_8252,
    egg_8252,
    (func_t)key_8680,
    egg_8680,
    (func_t)key_1291,
    egg_1291,
    (func_t)key_2814,
    egg_2814,
    (func_t)key_6777,
    egg_6777,
    (func_t)key_9820,
    egg_9820,
    (func_t)key_5422,
    egg_5422,
    (func_t)key_9850,
    egg_9850,
    (func_t)key_4134,
    egg_4134,
    (func_t)key_7282,
    egg_7282,
    (func_t)key_5329,
    egg_5329,
    (func_t)key_6582,
    egg_6582,
    (func_t)key_7031,
    egg_7031,
    (func_t)key_1059,
    egg_1059,
    (func_t)key_6154,
    egg_6154,
    (func_t)key_6221,
    egg_6221,
    (func_t)key_8907,
    egg_8907,
    (func_t)key_1183,
    egg_1183,
    (func_t)key_5785,
    egg_5785,
    (func_t)key_8411,
    egg_8411,
    (func_t)key_4536,
    egg_4536,
};
